syntax = "proto3";

package ru.otus.numbers;

option java_multiple_files = true;

message NumbersRequest {
    int64 firstValue = 1;
    int64 lastValue = 2;
}

message NumberResponse {
    int64 value = 1;
}

service NumbersService {
    rpc generateNumbers(NumbersRequest) returns (stream NumberResponse);
}
